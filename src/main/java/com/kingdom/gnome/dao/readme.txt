Здесь живет всё, что связано с хранением информации.
Этот слой ничего не знает про бизнес-логику и интерфейс.

Классы для работы с базой данных (SQL-запросы, подключение к БД).
Если будет использоваться ORM (например, Hibernate), здесь будут лежать репозитории.
Классы-сущности (Entity / Models) — например, User, Order, Product.
